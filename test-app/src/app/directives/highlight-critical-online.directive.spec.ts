import { HighlightCriticalOnlineDirective } from './highlight-critical-online.directive';

describe('HighlightCriticalOnline', () => {
  it('should create an instance', () => {
    const directive = new HighlightCriticalOnlineDirective();
    expect(directive).toBeTruthy();
  });
});
