import { HighlightCriticalDirective } from './highlight-critical.directive';

describe('HighlightCritical', () => {
  it('should create an instance', () => {
    const directive = new HighlightCriticalDirective();
    expect(directive).toBeTruthy();
  });
});
